#include <iostream>
#include <cmath> //добавяме библиотека за математически операции
using namespace std;

int main(){
    double x, y; //координати на точка
    cin >> x >> y;
    cout << sqrt(x*x + y*y); //sqrt - корен квадратен от изразa в скобите
}